# Fixture for `helm template` smoke testing. Use:
#
#   helm template test deploy/k8s/helm/crond-agent \
#     -f deploy/k8s/helm/crond-agent/examples/test-values.yaml
#
# These UUIDs are obviously fake — they exist so the rendered Secret has
# stable, predictable content for snapshot/golden-file comparison.
pingKeys:
  PING_KEY_BACKUP:  11111111-1111-1111-1111-111111111111
  PING_KEY_REPORTS: 22222222-2222-2222-2222-222222222222
